# Sparkline

A tiny equity line for table rows. The out-of-sample stretch is drawn in `partition-oos`, and a sealed holdout is left blank.

- Props: `series`, `oosFrom`, `holdoutFrom`, `sealed` (default `true`), `width` and `height`.

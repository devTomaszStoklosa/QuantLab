# PermutationTest

Answers "Is it luck?": a histogram of the statistic under shuffled signals, the observed value as a line and the p-value. The tail at or above the observed value is drawn in forge.

- Props: `nullDist`, `observed`, `pValue` (computed from the distribution if omitted) and `bins`.

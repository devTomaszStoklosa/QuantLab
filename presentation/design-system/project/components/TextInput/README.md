# TextInput

A single-line text input. Set `mono` for parameters, dates, seeds and IDs.

- Other props pass to `<input>`. Set `aria-invalid` with a `Field` error. Frozen values are `readOnly` and render sunken.

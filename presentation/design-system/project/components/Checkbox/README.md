# Checkbox

A checkbox with its label, used for explicit acknowledgements such as the holdout ceremony.

- Props: `label`. Other props pass to `<input type="checkbox">`. Write the label as a first-person commitment.
